install=> nodejs to use npm commands
Clone the 'my_react'folder
>npm start (to run the program on localhost)
'Dream City' blog will be opened. The blog contains articles on cities and links to different websites.
